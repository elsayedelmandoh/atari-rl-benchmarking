# evaluation

**propose:** store evaluation outputs -- predictions, metrics, and result figures.

**subdirs:**
- `predictions/` -- model-generated outputs from inference runs
- `figures/` -- plots, confusion matrices, performance charts

**input:** trained model from `artifacts/training/models/` + test data from `data/test/`.

**output:** scored predictions and publication-ready figures.
