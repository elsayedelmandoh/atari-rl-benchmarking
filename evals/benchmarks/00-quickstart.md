# benchmarks quickstart

**propose:** store standard eval suites and benchmark definitions used across experiments.

**input:** curated test sets, task definitions, scoring rubrics.

**output:** reusable benchmarks that compare model versions side-by-side.
